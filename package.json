{
  "name": "wav2amiga-web",
  "private": true,
  "packageManager": "pnpm@9.10.0",
  "engines": {
    "node": "20.17.0"
  },
  "volta": {
    "node": "20.17.0",
    "pnpm": "9.10.0"
  },
  "scripts": {
    "build": "pnpm -r --filter ./packages/* --filter ./apps/* run build",
    "lint": "pnpm -r exec eslint .",
    "test": "pnpm run test:unit && pnpm run test:golden:wasm",
    "test:unit": "pnpm -r --filter ./packages/core run test",
    "test:golden:wasm": "pnpm -r --filter ./packages/* --filter ./apps/cli run build && node tools/run-golden-tests.mjs --resampler wasm",
    "test:cli:ffmpeg": "pnpm -r --filter ./packages/* --filter ./apps/cli run build && node tools/run-golden-tests.mjs --resampler ffmpeg --structure-only",
    "versions:print": "node tools/versions.mjs",
    "goldens:regen": "node tools/regenerate-goldens.mjs"
  },
  "devDependencies": {
    "@eslint/js": "9.11.0",
    "@typescript-eslint/eslint-plugin": "8",
    "@typescript-eslint/parser": "^8.46.1",
    "eslint": "9.11.0",
    "eslint-config-prettier": "9.1.0",
    "globals": "^16.4.0",
    "prettier": "3.3.3",
    "typescript": "5.6.3"
  },
  "pnpm": {
    "overrides": {}
  }
}
